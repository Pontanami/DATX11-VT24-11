type Test {
    void runTest();
    void addSubscriber();
}
methods {
    void runTest() {
        TrafficController tc = TrafficController.new();
        GPS gps = GPS.new();
        tc add subscriber gps.onTrafficChange (RoadRepairEvent);
    }
}
