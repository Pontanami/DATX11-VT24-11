/**
 * The {@link java_builder} package contains classes and interfaces for generating java code using the builder pattern.
 * The common interface for builders is {@link java_builder.Code} which contains the 'build' method which is named
 * {@code toCode}. It generates the code as a {@link java.lang.String} using an {@link java_builder.Indentation} object.
 */
package java_builder;